# C2C Minimal Microservices

起動: `docker compose up --build`

初期ユーザー:
- test@test.com / pass (user)
- test2@test.com / pass (user)
- ops@test.com / pass (admin)

フロント: http://localhost:3000
API: http://localhost:{4000,4010,4020}
